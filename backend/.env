# MongoDB connection string — change to your MongoDB Atlas URI if needed
MONGO_URI=mongodb://localhost:27017/logguardian

# JWT Secret — change this to a strong random string in production
JWT_SECRET=supersecret

# Server port
PORT=5000
